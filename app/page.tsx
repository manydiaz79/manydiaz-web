usar cliente"

import { useState, useEffect } desde "react" import Navbar desde "@/components/barra de navegación" import HeroSection desde "@/components/sección-héroe" import QuienSoySection desde "@/components/quien-soy-section" import ProductosSection desde "@/components/productos-section" import TestimoniosSection desde "@/components/testimonios-section" import ContactoSection from "@/components/sección-de-contacto" import Footer from "@/components/pie de página" export default function Page() { const [activeSection, setActiveSection] = useState("inicio") useEffect(() => { const handleScroll = () => { constsections = ["inicio", "quien-soy", "productos", "testimonios", "contacto"] const scrollPos = window.scrollY + 100 for (const id de secciones) { const el = document.getElementById(id) if (el) { const top = el.offsetTop const bottom = top + el.offsetHeight if (scrollPos >= top && scrollPos < bottom) { setActiveSection(id) break } } } } window.addEventListener("scroll", handleScroll, { passive: true }) return () => window.removeEventListener("scroll", handleScroll) }, []) return ( <main className="min-h-screen"> <Navbar activeSection={activeSection} /> <HeroSection /> <QuienSoySection /> <ProductosSection /> <TestimoniosSection /> <ContactoSection /> <Footer /> </main> ) }
